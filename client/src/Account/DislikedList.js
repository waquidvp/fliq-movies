import React, { Component } from 'react';
import styled from 'styled-components/native';

const MainContainer = styled.View`
  flex: 1;
`;

class DislikedList extends Component {
  state = {};
  render() {
    return <MainContainer />;
  }
}

export default DislikedList;
